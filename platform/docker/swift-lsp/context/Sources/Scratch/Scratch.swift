import Foundation

// Anchor file. The web client edits this file (or virtual documents in this
// directory) so sourcekit-lsp serves completion against an indexed target.
public func scratchAnchor() {}
